model websiteScrapperSite {
  id    String  @id @default(cuid()) @map("_id")
  site           String
  description    String
  status   String
  seoPercentage  Float
  dateAt        DateTime   @db.Date
 
}

 